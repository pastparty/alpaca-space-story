import React from 'react';
import ReactDOM from 'react-dom';

const Menu = () => {
    return (
        <div>
            <h3>Menu</h3>
            <button>Save</button>
            <button>Delete</button>
            <button>Pause Game</button>
        </div>
    );
}

export default Menu;
