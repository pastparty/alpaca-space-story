import React from 'react';
import ReactDOM from 'react-dom';

const StatusBar = () => {
    return (
        <div>
            <small>StatusBar component</small>
        </div>
    );
}

export default StatusBar;
